# Stack root

This PR was the bottom of a stacked-diff series and has been merged. The rest
of the stack continues at the PRs listed below.

### Stack index

{{STACK_LIST}}

Maintained by `stack` from `{{MANIFEST_PATH}}`. Each PR above targets the
previous one; merge them in order from the top. Do not squash-merge
intermediate PRs (it breaks downstream rebase).
