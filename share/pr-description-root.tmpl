[Part {{ORDER}}] {{COMMIT_SUBJECT}}

{{COMMIT_BODY}}

---

### Stack index (root)

{{STACK_LIST}}

This is the root of a stacked-diff series produced by `stack` from
`{{MANIFEST_PATH}}`. Each PR above targets the previous one. Merge them in
order from the top. Do not squash-merge intermediate PRs (it breaks
downstream rebase).
